import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Esqueci3Page } from './esqueci-3.page';

describe('Esqueci3Page', () => {
  let component: Esqueci3Page;
  let fixture: ComponentFixture<Esqueci3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Esqueci3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
