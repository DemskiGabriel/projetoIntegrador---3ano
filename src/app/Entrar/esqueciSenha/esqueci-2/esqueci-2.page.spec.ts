import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Esqueci2Page } from './esqueci-2.page';

describe('Esqueci2Page', () => {
  let component: Esqueci2Page;
  let fixture: ComponentFixture<Esqueci2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Esqueci2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
